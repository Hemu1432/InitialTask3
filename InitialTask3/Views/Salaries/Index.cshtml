@model IEnumerable<InitialTask3.Models.Salary>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LastYearOtherIncome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalIncome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastYearOtherIncome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalIncome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActualAnnualSalary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActualAnnualSalary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActualAnnualSalary">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
